def appearances_words(my_text):
    '''Build a data structure that points out how many times is each word
    contained in a text.

    my_text (string): any text

    Output (dictionary): key = word, value = number of times
    '''
    result = dict()
    #Usar .split para saber cada palabra de la cadena
    if key not in 
    return result
